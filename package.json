{
  "name": "remop-retifica",
  "private": true,
  "description": "Site estático da Remop Retífica de Motores — dependências usadas só pela Netlify Function do assistente de IA.",
  "dependencies": {
    "firebase-admin": "^12.6.0"
  }
}
